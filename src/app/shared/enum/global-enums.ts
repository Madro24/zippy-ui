export enum ServiceTypeEnum {
  Express,
  Normal
};

export enum PayByEnum {
  Origin,
  Destination
};

export enum ServiceStatusEnum {
  Created,
  Started,
  InProgress,
  Completed,
  Archived
};
